# Directory containing your photos (required)
PHOTO_DIR=/path/to/your/photos

# API URL as seen from the browser (default: localhost:9002)
# Change this if accessing from another machine on the network (e.g. http://192.168.1.10:9002)
# API_BASE_URL=http://localhost:9002
