# .env files are NOT used for app configuration in this project.
#
# API base URL is injected at runtime via window.__env__.API_BASE_URL:
#   - Production : docker-entrypoint.sh reads the API_BASE_URL container env var
#   - Local dev  : copy public/env.js.example → public/env.js and set the URL there
