// Copy this file to public/env.js (gitignored) for local API-mode development.
// Without this file the app runs in mock mode (public/mock-data/photos.ndjson).
window.__env__ = {
  "API_BASE_URL": "http://localhost:8080"
};
